# 2026-09-09 工作日志

## flame_human 新仓搭建（承接 09-08 的人脸链路 v2 评审）

用户定了目录名 `flame_human`，按 AGENTS.md 规范从零铺脚本，分步 commit。
设计依据：`vggt_human/DESIGN_face_pipeline.md`（09-08 评审产出，6 个分支全收敛）。

### 已完成（4 次 commit，均未 push）

| commit | 内容 |
|---|---|
| `9f8371f` | 骨架：`_env.sh` / `00a_setup_env.sh`(WSL) / `README.md` |
| `00e4dfb` | `01_download_models.sh` / `01b_build_lm468_embedding.sh` + `build_lm468_embedding.py` / `download_urls.md` |
| `f916576` | `02_detect_faces.sh`+`detect_faces.py`（阶段一）/ `03_match_faces.sh`+`match_faces.py`（阶段二三层判据）|
| `3b53c93` | `colmap_io.py` / `04_recon_faces.sh`+`recon_faces.py`（阶段三）|

### 关键实现决策

- **阶段一不做 mask 引导**（与 vggt_human 的 detect_face_landmarks.py 相反）：
  全图直接检测，归属交给阶段二三层判据。理由：mask 引导会把 mask 自身错误带进来。
- **阶段二代价** `cost = -(INSIDE_W·inside + iom)`，INSIDE_W=2.0 让 point-in-mask 压过 IoM；
  逐帧 `linear_sum_assignment` 一对一；再做时序连续性检查（MAX_JUMP_PX=200，
  只标记不删，DROP_SUSPECT=1 才删）。
- **各向同性裁剪**：`square_crop()` 边长 L = max(w,h)×scale_exp，越界先
  `cv2.copyMakeBorder(BORDER_REPLICATE)` pad 整图再裁（**不用黑边**），
  逆变换 `u_full = (u_crop/size)·L + cx - L/2`，与 pad 量无关。
- **CROP_SCALE_LM=1.6**（对齐 vggt_human PAD_RATIO=0.6），**CROP_SCALE_DECA=1.25**（对齐 DECA 官方 demo）。
  注意参考实现用 2.5（含上胸）是给自研 ExpTracker 的，MediaPipe 468 不需要，
  2.5 会把脸压到 512 canvas 的 205px，精度损失——这里是**有意改小**。
- **PnP 位姿初值不依赖 DECA**：用 lm468 嵌入取 FLAME mean face 上的 468 个 3D 点 +
  2D 观测解 `solvePnPRansac`。DECA 只是可选加分项（加载失败自动退回）。
- **DECA 维度对齐**：DECA 默认 n_shape=100 / n_exp=50，FLAME 2020 是 300/100，
  `_pad_to()` 补零。输入归一化用 [-1,1]（对齐 DECA datasets 的 Normalize(0.5,0.5)）。
- **lm468 嵌入自己生成**（`build_lm468_embedding.py`）：MediaPipe canonical_face_model.obj
  → 归一化 → 暴力搜旋转粗对齐（yaw 全周 × pitch × roll）→ ICP 精修 → 最近三角形取重心坐标
  → 鼻尖/眼/嘴语义自检。不依赖第三方转存的 NPZ。

### 踩坑
- **Write 工具在 Windows 下写的行尾**：git 检查显示 `.gitattributes` 已设 `eol=lf`，
  实际写入是 LF（用 `grep -c $'\r'` 检查会误报——空模式匹配所有行，
  必须用 python 读 bytes 判断 `b'\r\n'` 才准）。
- 每次 commit 用 `git -c gc.auto=0 -c maintenance.auto=false`（AGENTS.md §11 硬性要求）。

### 后续完成（阶段四~七）

| commit | 内容 |
|---|---|
| `84bc326` | `05_align_3dmm.sh` + `align_3dmm.py`（阶段四三阶段优化，最核心）|
| `e764261` | `06_init_avatar_gs.sh` + `init_avatar_gs.py`（阶段五 bary 绑定）|
| `1df273b` | `07_init_body_gs.sh` + `init_body_gs.py`（阶段七 point-to-triangle + 补缝）|

**阶段四要点**：
- 尺度初始化用**线性最小二乘解 SfM↔米**：每帧 `R_cam·p_head + t_cam = s·t_pnp`，
  未知数 [p_head(3), s(1)]，3F 方程解 4 未知数。不能直接混用 t_cam(SfM) 与 t_pnp(米)。
- 4.1 时 local/id/exp **全部冻结**（run_stage 的 optimize 字典控制），
  4.2 放开 local 且 global 走 0.1× lr，4.3 全放开。
- `median_quat()` 返回必须统一成 [w,x,y,z]（scipy as_quat 出来是 [x,y,z,w]，易错）。
- reject 在 4.2 之后，`thr = max(3.0, median(rms) × 5.0)`，剔除后重跑 4.2 再进 4.3。

**阶段五要点**：`area_weighted_sample` 保留 `(face_id, bary)`（**注意：vggt_human
的 build_head_gs.py 把它丢了**）；自由高斯只从「y 分位数以上」的头皮区采样并沿法线外推，
脸上放自由高斯会和绑定点打架。

**阶段七要点**：`surface_distance()` 优先 trimesh ProximityQuery（点到表面，
与顶点密度无关），退回 KNN。**阈值/外推都用米制毫米给，乘各自 scale 转世界单位**，
否则 verts（世界单位）和 offset（米）直接相加会错。

### 收尾（训练 + 一键流程）

| commit | 内容 |
|---|---|
| `6ab4f9f` | `08_train.sh` + `train_avatar.py`（阶段六，最后一个大块）|
| `b7de5b2` | `09_move_output.sh`（WSL 搬运）+ `run_all.sh`（一键 02→08）|

**训练脚本要点**：
- 复用官方 `gaussian_renderer.render`，模型只需提供
  `get_xyz/get_opacity/get_scaling/get_rotation/get_features/get_covariance` 接口；
  `get_xyz` 返回**当前帧形变后**的世界坐标（`set_frame(f)` 切换）。
- 官方 Camera 的 `R` 要传 **c2w**（= `qvec2rotmat(qvec).T`），它内部会再转一次回 w2c。
  `pipe` 不用 argparse，传 `SimpleNamespace(compute_cov3D_python=False,
  convert_SHs_python=False, debug=False)` 即可。
- **bary 用 softmax 参数化**（存 log 再 softmax）：恒为合法重心坐标 + 可训练。
  硬绑定下 clone 出的两点初始完全重合，只能靠 bary/scale 分开，所以 bary 必须能学。
- `fix_and_sync_exp()`：取 `median(exp)` 的**最近真实帧**（不是中位数向量本身，
  逐维中位数可能拼出怪表情），`requires_grad_(False)` 冻结。
- densify 的梯度统计沿用官方（可见点 screen-space 梯度范数），加了长度校验。

### flame_human 仓状态
23 个文件，`00a`→`09` + `run_all`，全部 `bash -n` / `py_compile` 通过，
工作区干净。**全部未 push**（用户手动推）。

### 未实现 / 待办
- `00_setup_env.sh`（服务器版）只有 WSL 的 `00a`。
- 06/07/08 都只处理单个 `PID`（默认 00），多人要循环调用。
- **08 只训 head avatar**：07 产出的 body 高斯没有对应训练循环，
  头/身/场景联合 composite 渲染（对标 vggt_human 的 03j）整块缺失。
- 阶段五增强三入口（前处理增强 / epoch48 后处理增强 / novel view 注入）未实现，
  08_train 只留了触发钩子（`ENHANCE_TRIGGER_epoch48.json` + checkpoint）；
  HYPIR 在 flame_human 里完全没有集成。
- 全部代码**未经真实数据验证**。

### 盘点结论（09-09 补）
排期内 6 任务 + 追加的 08/09/run_all 全部完成，共 12 个 flame_human 相关 commit
（含 2 个记忆 commit），工作区干净，全部未 push（用户手动推）。

## 端到端跑通（09-09 下午场）

### proxy.env 项目专属命名改造（用户拍板）
- 通用名污染根源：vggt_human 00a 生成的 proxy.env 无条件 `export MODEL_DIR`
  （指向 vggt_human_ms/VGGT-Omega），`set -a` source 后覆盖 flame_human
  `_env.sh` 的 `${MODEL_DIR:-default}`，命令行显式传参也救不回来。
- 方案：**不删 proxy.env**（vggt_human 依赖），改为项目专属名：
  `VGGT_HUMAN_MODEL_DIR` / `VGGT_HUMAN_RESULTS_DIR` / `FLAME_HUMAN_MODEL_DIR`
  （`/mnt/d/wheel/flame_human_ms`） / `FLAME_HUMAN_RESULTS_DIR`。
  `WEIGHTS_ROOT` 保持通用（HYPIR/sam2/DiffBIR 多项目真共享）。
- 同步改了：proxy.env、vggt_human/_env.sh:81,86、vggt_human/00a heredoc（新机器
  不再写回旧通用名）、flame_human/_env.sh:91,115、flame_human/README.md。
- vggt_human 各分步脚本仍是 `RESULTS_DIR="${RESULTS_DIR:-...}"` 读法，
  `_env.sh` 导出链不变，行为无变化（验证过 grep）。

### 00a 首跑 37min 的坑与修（新增 00b_fix_env.sh）
| 问题 | 根因 | 修法 |
|---|---|---|
| numpy 2.2.6 vs torch/chumpy | torch 2.1.2 按 numpy 1.x 编译；chumpy 用旧别名 | 降 1.23.5（opencv5/plyfile/skimage 元数据警告可忽略，import 实测过） |
| torchvision ImportError COMMON_CJK_CHARACTERS | gdown 曾装 csn 3.x，覆盖装 2.1.1 后残留 3.x mypyc .so 混装 | 删 site-packages/charset_normalizer* 后 --no-cache-dir 重装 2.1.1 |
| DECA clone 到 /mnt/c/code/DECA | 00a 启动时 ~/repos/DECA 尚在 clone（预 clone 竞态），_env.sh 判定走 drvfs | 00b 里 mv 到 ~/repos/DECA；误 clone 份已删 |
| pytorch3d wheel 404 | 00a URL 版本号 pyt1120 错（应 pyt2120），且 fbaipublicfiles 被墙 | 走 ghfast.top 镜像 clone 源码编译 |
| pytorch3d metadata 失败 | setuptools 83 删了 pkg_resources（torch 2.1.2 cpp_extension 还在用） | 降 setuptools<81 |
| pytorch3d 编译失败 ×2 | ①缺 g++（系统无编译器）②conda gcc 15.2 太新（CUDA 11.8 nvcc 最高支持 gcc 11）③缺 cusparse.h | conda 装 gcc/gxx=11 + cuda-11.8.0 channel 补 libcusparse-dev/libcublas-dev/libcurand-dev + bin 下建 gcc/g++ 符号链接 |
- verify_env.py / verify_pytorch3d.py 留在仓里做幂等自检；00b 可重复执行。
- 00a 里同步修了 3DGS 子模块：`--no-build-isolation` + MAX_JOBS=8。

### 环境最终状态（全绿）
numpy 1.23.5 / torch 2.1.2+cu118 / torchvision 0.16.2 / chumpy 0.70 / mediapipe
1.0.1 / smplx / cv2 / scipy / plyfile / skimage / trimesh 全 OK，CUDA 3090 可用。
pytorch3d 0.7.9 源码编译成功（knn + point_mesh_face_distance 实测过）。
3DGS 子模块（diff-gaussian-rasterization + simple-knn）编译成功（rasterizer
构造 + distCUDA2 实测过）。
- ⚠️ pytorch3d 0.7.9 的 `point_mesh_face_distance` 在 `pytorch3d.loss` 不在
  `pytorch3d.ops`（07 用 trimesh ProximityQuery 不受影响）。
- ⚠️ 3DGS 子模块首装 6 秒完成是**复用了 pip 缓存里 vggt_human env（torch 2.3）
  编的 wheel**，import 报 undefined symbol throwNullDataPtrErrorEv —— 必须卸载后
  `--no-cache-dir` + 删 build/ 重编。00b 里已用 --no-cache-dir。

### 02 的输入数据核对（待跑）
- verify 数据集：`~/output/vggt_human_verify/source/`（125 张 images + sparse）✅
- ⚠️ `~/output/vggt_human_results/03_source/images` 是空的（别用这个）
- person mask 已在 `~/output/flame_human_results/01c_sam3_person_masks`（597 文件）
- FLAME/DECA 权重仍缺（硬阻塞 01b/04/05）：
  - FLAME2020 generic_model.pkl → 官网注册下载
  - deca_model.tar → DECA README 的 Google Drive

### 模型复用结论（回答用户）
- ✅ 可复用：SAM3/SAM2/HYPIR/3DGS（D 盘 wheel + ~/repos 均在）。
- ❌ 必须新下：FLAME2020 generic_model.pkl（官网注册）、DECA deca_model.tar（Google Drive）。
  ICT-FaceKit 是自研旧链路（MHR 模型），非 FLAME，不能用。
- 数据源：verify 用 `3fe060...`（125 张 jpg），非用户提的 `10e3ec...`（150 张 heic）。
  person mask 已由 01c 生成（3 人 298 张，video mode 39.7s）。

## 新增 99b_pack_ply_to_mp4.py（PLY → MP4 批量封装）

- 配套 99a（收集 ply）。输入粒度：遍历**原始批次目录** `<batch>/<task_id>/`，
  每个 task 里需同时有 ply + 图片序列 + init_camera.json / camera.json /
  view_limits.json，产出 `<OUT_DIR>/<task_id>.mp4`（默认 `<SRC_ROOT>_mp4`）。
- 四步链：ffmpeg 图片序列→H.264 / `encode.py` PLY→GSCompressed_B4.bin /
  `build/gltf_packer` →3DGS.glb / `muxer.py` →最终 mp4。工具链 `TOOL_DIR`
  默认 `/data_3d/w00950754/model/UWA_Sample_Tool_v3`。
- 文件名**自动探测可覆盖**：ply 按 recon_result/point_cloud_final/... 候选，
  图片目录 image/images/input/frames，扩展名+位宽+起始编号从文件名统计出来。
- 两个易错点已内建处理：
  ① ffmpeg `%0Nd` 模式默认 start_number=0，序列从 1 编号会报
  "Could find no file"，必须显式 `-start_number`；
  ② yuv420p 要求偶数宽高，加了 `pad=ceil(iw/2)*2:ceil(ih/2)*2`。
  ③ encode.py 的 ASTC 块大小由它内部决定，期望的 B4.bin 不存在时 glob B*.bin 兜底。
- 默认 KEEP_WORK=1 保留中间产物（排错用），FORCE=1 覆盖已有 mp4，DRY_RUN/ONLY 支持。
- 已在 Windows 上用假数据干跑验证探测逻辑（Linux 侧 ffmpeg/工具链未实测）。

### 双模式判断（UWA / COLMAP，commit 9fe7887）
- 两种 task 目录布局混在一个批次里，脚本自动判断并打印：
  - **UWA**：`init_camera.json + camera.json + view_limits.json + recon_result.ply` → 直接跑四步链
  - **COLMAP**：`cameras.txt + images.txt + points3D.txt + processing.txt + point_cloud_final.ply`
  - 两者都有 → 以 UWA 优先（json 齐全才能封）；`MODE=uwa/colmap` 可强制
- **COLMAP 模式不擅自封装**：三个 json 的字段名拿不到真样本（工具链只在服务器
  `/data_3d/w00950754/model/UWA_Sample_Tool_v3`，本机+WSL 都没有，网上也搜不到），
  所以只做「解析 + 打印统计 + 落待校验骨架 json」（`<work>/uwa_json/`，带 `_note`），
  状态记为 `⏸️ pending`，与真失败 `❌` 分开统计。
  给了 `REF_JSON_DIR`（真实 json 目录）就直接借用并继续封装。
  真样本到手后改 `build_uwa_json_skeleton()` 一个函数即可。
- **COLMAP images.txt 解析坑**：位姿行固定 10 列、其后 points2D 行**可能是空行**。
  先滤空行再按「两行一张图」跳读会把张数读成一半。判定规则改为
  `列数>=10 且 列数%3!=0`（points2D 行恒为 3k 列）。
- Windows 下用 python `write_text()` 改文件会把 LF 变成 CRLF，改完必须
  `read_bytes().replace(b'\r\n', b'\n')` 写回。
