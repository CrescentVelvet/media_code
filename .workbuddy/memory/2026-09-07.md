# 2026-09-07 工作日志

## vggt_human：3DMM 头/身/景拆分（目标架构，新建）

动机（接 09-06）：闭环推近的 40% 覆盖率目标在当前「椭球 K×spread」头部代理下
物理不可达（椭球半轴 8.8cm < 真头半径 ~9cm，推到 0.06m 相机进到高斯簇内部 cov 塌 0）。
根子在 head_gs 不是真的人头 → 必须上 3DMM。

### 进度（4 步里完成 3 步）

| 步骤 | 脚本 | 状态 |
|---|---|---|
| 03d 模板准备 | `03d_prepare_3dmm.sh` + `prepare_3dmm_template.py` | ✅ b2ba329 |
| 03e landmark 检测 | `03e_detect_landmarks.sh` + `detect_face_landmarks.py` | ✅ de7d058 |
| 03f 多视角拟合 | `03f_fit_head_3dmm.sh` + `fit_head_3dmm.py` | ✅ 7ee9c88 |
| 头网格→head_gs.ply + body/scene 拆分 | 未开始 | ⏳ |

### 选型：ICT-FaceKit（MIT）

FLAME / BFM / SMPL-X 都要签协议或注册，**ICT-FaceKit（USC-ICT/ICT-FaceKit，MIT）
是唯一能直接 curl 下来的完整头模型**（含后脑、脖子，26719 顶点 / 26384 面）。
- `generic_neutral_mesh.obj` 通用头；`identity000-023` 24 个身份 → identity PCA（20 基）
- skin 段（README 公开拓扑）：Face[0:9409] + Head&Neck[9409:11248] 顶点、面 [0:11144]
  → 11248 顶点 / 11144 面，排除眼球/牙齿/舌头/睫毛等内部件
- 尺度是真实 cm（bbox 24.83×33.81×22.32）

### landmark 对应：MediaPipe canonical 468 ↔ ICT skin

MediaPipe 的 468 landmark 与 `canonical_face_model.obj` 顶点**索引一一对应**，
所以拿 canonical 当"前脸点云"，配到 ICT skin 上就得到 468↔ICT 的 barycentric 对应。
配准流程：鼻尖/头顶/下巴 3 锚点初始化（yaw 0/180 双假设）→ 纯刚性点面 ICP。
**结果**：median 0.122cm、鼻尖前向排名 2/468、眼距比 0.998 / 面高比 1.000。

### 拟合结果（03f，150 帧 / 3 人）

| pid | 观测帧 | s | RMS 重投影 | 头中心 | R_p90 |
|---|---|---|---|---|---|
| 00 | 44/135 | 0.003684 | 1.55 px | [0.125,-0.042,0.601] | 0.039 |
| 01 | 78/112 | 0.003809 | 1.56 px | [-0.148,-0.080,0.613] | 0.041 |
| 02 | 100/149 | 0.004103 | 2.57 px | [0.031,-0.243,0.758] | 0.042 |

**强验证**：1 世界单位 = 1/0.0038 cm ≈ 263cm → 头半径 0.041 ≈ **10.8cm**（真人 9~11cm），
且三人一致；重投影 1.5~2.6px。说明 VGGT 世界尺度被正确解出。

### 踩坑（都是 ICP / IO 层的，值得记住）

1. **ICP 里 src 不能每轮换**：把 `P` 当下轮 src → 40 轮内塌缩成一点（P_std 4cm→0.3cm）。
   正确：src 固定为 MP canonical，每轮 `P = T@src` 求最近点。
2. **`T = A @ T` 双重应用**：Umeyama 的 src 已固定时 A 本身就是完整变换，
   应 `T = A`（替换）。写成累积会让 med_d 从 0.15cm 炸到 28cm。
3. **带尺度 Umeyama 会退化**：C 的奇异值退化时 s→0（实测 0.0005），
   每轮 s 略<1 累积 40 轮就塌缩。两个模板都是真实 cm 级 → **全程锁 s=1**。
4. **`np.diag(S) @ np.diag(D)`**：D 是 2D 数组，`np.diag(D)` 取的是对角线（1D），
   正确写法 `s = (S * np.diag(D)).sum() / var`。
5. **identity*.obj 混有 quad 面**：`load_obj` 三角化会打乱 generic 的面段顺序
   （skin 段是按多边形数给的）→ generic 必须 `triangulate=False`。
6. **proxy.env 的 `WEIGHTS_ROOT=/mnt/d/wheel/...` 是遗留值**，且 `_env.sh` 用
   `set -a` source 会盖掉外部传入值 → **不要拿 WEIGHTS_ROOT 拼路径**。
   `RESULTS_DIR` 在 proxy.env 里写的是 `${RESULTS_DIR:-...}`，外部可覆盖（安全）。

### 待办

1. `build_head_gs.py`：头网格 → 高斯（三角面内 densify + 多视角颜色采样 + 写 GS PLY）
2. body/scene 拆分：需要 **SAM3 person mask**（现有只有 face mask）→ 先跑一次
   prompt=person 的分割，再按「SfM 点投影落在 person mask 内 + 距头中心 > 头包围盒」拆
3. 集成回 `render_closeup.py`：`select_face_gaussians` 换成 head_gs，TARGET_COV 调回 40
4. p00 检测率只有 33%（侧背脸），如需补帧可用 person mask 轮廓约束（v2）

### 环境备注

- 我看不了图（模型不支持图像），所有对齐验证都改成**数值指标**
  （鼻尖前向排名、眼距/面高比、落面残差、重投影 RMS）——这套指标很管用，以后沿用
- WSL vggt_human env：mediapipe 0.10.14（`mp.solutions.face_mesh` 可用）、
  matplotlib 3.10.9 有，**sklearn 没有**（PCA 手写）

## 03g head_gs + render_closeup 接入（下午续）

### 完成内容（commit 90b722d / 36a40a6）
1. `build_head_gs.py` + `03g_build_head_gs.sh`：头网格 → 60k 高斯。
   取色三级策略：mask 内观测（≥3 帧取中位）→ 仅可见性观测中位 → 无观测近邻补色
   （p00 约 1/4 点是后脑/脖底，相机从没看到）。各向异性薄片高斯（σ_t=0.5×点距，
   σ_n=0.25×点距，法向对齐），点距 ≈1.2mm。输出 `head_gs_p*.ply` + vis 拼图。
2. `render_closeup.py` 接入：probe 与 geo mask 优先渲染 head_gs（HEAD_GS=auto），
   face_center 改用 head_fit 的头中心（几何自洽）。
3. 用户工作方式：**AI 看不了图但用户能看 → 产出对齐/结果图像后把路径发用户检查**。

### ⭐ 重大发现：diff-gaussian-rasterization 硬编码 near=0.2
- `cuda_rasterizer/auxiliary.h:166: if (p_view.z <= 0.2f)` —— 深度 <0.2 世界单位的
  高斯**全部被剔除**。
- 症状：head_gs 距离扫描 d=0.2 时 cov 4.4%，d≤0.18 直接 **0%**（头深度全 <0.2）。
- 修复：改 0.01f + `pip install -e . --no-build-isolation` 重编译（1 分钟，CUDA 12.1）。
  原文件备份 `auxiliary.h.bak_near02`。
- **09-06 的「40% 覆盖率天花板 = 相机进高斯簇 cov 塌 0」一半根因在此**：
  不是物理不可达，是 near 裁剪把头表面削掉了。修复后 d=0.125 即可达 40%。
- 训练影响：closeup 训练帧距头 0.10~0.14，修复前头近侧在训练渲染里是缺失的。

### 参数调整（render_closeup 默认值）
- MIN_DIST_RATIO 0.3→0.15、MIN_DIST_ABS 0.15→0.10（头半径 0.04 + 6cm 表面距）
- 达标容差 0.05%→0.5%（迭代上限退出时 39.5% 也算达标）

### p00 验证结果（8 视角，TARGET_COV=40）
- probe 覆盖率 38.5~40%（修复前最高 14.6%），物距 0.10~0.14m，全部对准人脸
- 512 渲染 alpha_cov 29~33%（分辨率差异：128 probe 略高于 512 全图）
- 侧脸视角（0003-0006）推到 0.10~0.11m 后 38~39.5%

### 待办
- [ ] 用户看图确认 head_gs overlay 与 closeup 渲染质量
- [ ] p01/p02 全量跑 closeup（head_gs 模式）
- [ ] body/scene 拆分（需 SAM3 person mask）
- [ ] MAX_PUSH_ITERS=3 可到 5（部分视角差 0.5% 被迭代上限卡住）
