# 2026-09-11

## 新增 99d_copy_three_frames.py（抽 3 帧并重命名）
- 需求：从 `../../code/Reconstruction/dataset/B003_Human_Data_w_pose` 的每个 task
  子目录 `<task>/image/` 里抽 01000000/01000050/01000100.jpg，按 task 名重命名，
  拉平到 `../../output/recon_human_results/img_three`
- 参照 99a_collect_ply.py 的结构（单函数 + main 里写死路径 + emoji 日志 + DRY_RUN）
- **命名默认 `orig`**：`<task>_<原帧号>.jpg`（信息无损，可从文件名反查源帧）；
  `SUFFIX_STYLE=seq` 可切 `<task>_1/2/3.jpg`（用户原话「子文件夹名 + 序号后缀」有歧义，
  两种都留了开关）
- 可覆盖 env：SRC_ROOT / RESULTS_ROOT / DST_ROOT / FRAMES / IMAGE_SUBDIR /
  SUFFIX_STYLE / NAME_SEP / ONLY / DRY_RUN
- 缺帧不中断：逐帧跳过，任务级打印「缺 N/3 帧」，末尾汇总「不完整」清单
- 已用 C:/tmp 假数据集验证（含缺帧 task 与无 image/ 的 task），三种模式输出正确
